---
- hosts: all 
  name: Install the infastructure needed to take the final. 
  become: true 
  tasks:
    - name: Install pip 
      apt:
        update-cache: yes
        pkg:
        - python3-pip 
    - name: Install my function library. 
      pip:
        executable: /usr/bin/pip3
        name: http://www.lifealgorithmic.com/dist/lifealgorithmic-0.0.2.tar.gz
